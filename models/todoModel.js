const mongoose = require("mongoose");

// Creating a scheme for TODO
const todoSchema = new mongoose.Schema({
  title: {
    type: String,
    required: true,
  },
  description: {
    type: String,
    required: true,
  },
  createdAt: {
    type: Date,
    required: true,
    default: Date.now(),
  },
  updatedAt: {
    type: Date,
    required: true,
    default: Date.now(),
  },
});

// export
const Todo = mongoose.model("Todo", todoSchema);
module.exports = Todo;
